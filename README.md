# 🧮 Calculator (Python)

A simple yet powerful **command-line calculator built in Python** that supports multiple arithmetic operations and allows users to perform continuous calculations in an interactive loop.

This project is created to practice **Python fundamentals**, including functions, conditionals, loops, and error handling.

---

## ✨ Features

- Supports multiple operations:
  - Addition (`+`)
  - Subtraction (`-`)
  - Multiplication (`*`)
  - Division (`/`)
  - Power (`^`)
  - Percentage (`%`)
- Handles division by zero safely
- Continuous calculation using a loop
- Input validation with `try-except`
- Clean and user-friendly command-line interface

---

## 🛠️ Technologies Used

- Python 3
- No external libraries required

---

## 🚀 How to Run the Project

1. Clone the repository:
   ```bash
   git clone https://github.com/rehan9623/Calculator.git
